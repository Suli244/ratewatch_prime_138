import 'package:flutter/material.dart';

class ProfitScreen extends StatelessWidget {
  const ProfitScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ProfitScreen'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}
